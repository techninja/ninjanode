import shipExplosion from './shipExplosion.json' with { type: 'json' };
import poof from './poof.json' with { type: 'json' };
import thrust from './thrust.json' with { type: 'json' };

export const emitters = {
  poof,
  shipExplosion,
  thrust,
};
