/**
 * SFXR Sound definitions, loaded from JSON.
 * @goto https://sfxr.me/ to edit these sounds dynamically.
 */

import thrustGen from './thrustGen.json' with { type: 'json' };
import powerup from './powerup.json' with { type: 'json' };

export const sfxrSounds = {
  thrustGen,
  powerup,
};
